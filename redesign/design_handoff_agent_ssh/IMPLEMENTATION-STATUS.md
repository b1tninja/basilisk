# Agent + SSH — implementation status

Checklist for building [AGENT-SSH-DESIGN.md](./AGENT-SSH-DESIGN.md), in
dependency order: pure encodings first (testable against `ssh-keygen` with
no UI), then vault multi-kind, then the boundary ops + approval UI, then
the CLI socket agent. Every unit lists acceptance criteria measurable by
test or by browser measurement (`getComputedStyle`/DOM, per HANDOFF —
screenshots are unreliable here).

**Status: units 1-5 are built and committed** (`72ebd78`, `17b54a4`,
`f622203`, `d431071`, `2be2878`). Boxes below are ticked to match. Where
the build departed from the design, the deviation is recorded inline as
**Deviation:** rather than quietly leaving a box ticked — a status doc that
disagrees with the code is worse than no status doc.

Engine capabilities this design needs that do not exist today are called
out inline as ⚙ — do not discover them mid-implementation.

## 1. SSH encodings (pure JS, no UI) — §29

- [x] **`lib/ssh/wire.js`** — RFC 4253 public blob encode/decode
  (string/mpint/uint32 fields) for ssh-ed25519, ecdsa-sha2-nistp256/384/521,
  rsa-sha2. Accept: round-trips fixtures generated by `ssh-keygen` for all
  five algorithms, byte-exact both directions; malformed blobs fail with a
  named field, not an offset.
- [x] **`lib/ssh/openssh-key-v1.js`** — unencrypted private container
  encode/decode (`cipher=none`, `kdf=none`). Accept: decodes an
  `ssh-keygen -t ed25519 -N ""` file to a working key; encodes one that
  `ssh-keygen -lf` fingerprints identically; a bcrypt-encrypted file is
  refused with the §29f message verbatim (string-asserted, the
  `share-check.js` precedent).
- [x] **`lib/ssh/fingerprint.js`** — SHA-256 over the public blob,
  `SHA256:` + unpadded base64. Accept: equals `ssh-keygen -lf` output for
  every fixture.
- [x] **`lib/ssh/sshsig.js`** — sshsig encode/decode/sign/verify
  (namespace, hash sha512|sha256, armor). Accept: verifies a checked-in
  `ssh-keygen -Y sign` fixture; ed25519 sign output is byte-identical to
  `ssh-keygen`'s for the same key/payload/namespace (deterministic per
  RFC 8032); namespace mismatch fails with the §31c message; tampered
  payload fails.
- [x] **Fixtures** — `ssh-keygen`-generated keys, public lines,
  fingerprints, and sshsig blocks checked in under `src/test/fixtures/ssh/`
  with a README noting the generating commands. Accept: fixtures load in
  Vitest with no network and no local `ssh-keygen`.

## 2. `ssh.*` registry ops — §29

- [x] Register `ssh.encode` / `ssh.decode` / `ssh.fingerprint` /
  `ssh.sign` / `ssh.verify` exactly per §29d (params, overloads, docs);
  `ssh` toolbox at order 6 (later toolboxes shift +1), `sshwire`/`sshsig`
  shelves. Accept: `list-ops --toolbox ssh` shows five ops; the
  `namespace.singlelowercaseword` convention test passes.
- [x] ⚙ Refined text kinds `ssh-public` / `ssh-private` / `sshsig` in
  types.js + type-registry cards. Accept: `ssh.encode | ssh.decode`
  type-checks; type browser lists real producers/consumers.
- [x] CAST mapping: `toolboxToSuite("ssh") → "webcrypto"`. Accept: the SSH
  header CastDot renders the webcrypto suite state (measured on
  `/toolkit-widgets`).
- [x] Toolbox accent `#39c5cf` in `TOOLBOX_META` **and** the enumerated
  `toolkit.css` rules. Accept: `toolbox-dot-css.test.js` passes;
  `getComputedStyle` on the SSH dot matches.
- [x] Verb smoke: every op + every enum value + `-q` in compiling, running
  recipes. Accept: `recipe-verbs.test.js` passes with no catalog waiver.
- [x] Glyphs `ssh`, `ssh-key`, `sshsig-sign` per §29e metaphors
  (`web/glyphs/svg/`, manifest, `npm run glyphs`). Accept: drawer renders
  them (no `#` fallback glyph on any ssh row).
- [x] Unencrypted-export warning: compile warning on
  `ssh.encode format=private` + warn row on the artifact tile with §29f
  copy; output `sensitive: true`. Accept: warning text asserted in a
  compile test; tile masks until revealed (catalog state).
- [x] CLI: all five ops run headlessly. Accept: a CLI test signs and
  verifies via `basilisk run` with exit 0.

## 3. Vault multi-kind — §28

- [x] Record `kind` field (absent = pgp), `publicLine` for ssh kind;
  schema bump in `vault.js`. Accept: legacy records list as pgp untouched;
  a saved ssh key round-trips kind + publicLine.
- [x] ⚙ Kind-aware id handling — `normalizeVaultFingerprint` strips
  non-hex today and would destroy `SHA256:` base64 ids; every caller
  (vault, vault-unlock, vault-session, agent-ops, recipient matching)
  audited. Accept: unit tests for both id shapes; PKESK matching
  (`vaultKeyMatchesRecipients`) unaffected for pgp keys.
- [x] `agent.save` accepts `keypair` input → kind ssh (mappable alg) or
  raw; stores openssh-key-v1 / PKCS#8 payloads.
  **Deviation:** symmetric keys (aes, hmac) are refused, not stored as
  raw. They have no public half, so a vault row for one would be a label
  over nothing the listing can display, and no id scheme fits them. Accept:
  `genkey ed25519 | agent.save` lists as ssh with a correct fingerprint;
  `genkey x25519 | agent.save` lists as raw; passphrase protection for
  non-pgp kinds is refused with the §28b message (not silently downgraded).
- [x] `agent.unlock` on non-pgp kinds emits `keypair`. Accept:
  `agent.unlock SHA256:… | ssh.encode` runs; §26c exposure treatment
  applies (tile masked).
- [x] `agent.list` gains `kind` (+ `publicLine` for ssh). Accept: JSON
  asserted; CLI output unchanged in shape otherwise.
- [x] Listing UI — My Keys page (`my-keys-mount.js`) and Keyring tray:
  kind badges via `data-key-kind` + enumerated CSS, kind-formatted ids,
  ⧉ public-line copy on ssh rows, disabled passphrase radio with honest
  copy. Accept: measured in catalog + live page (`getComputedStyle` on
  badge tones); copy button writes the exact public line (clipboard test
  with real input event, per HANDOFF's transient-activation note).
- [ ] `agent.list` tile renders rows (kind badge, id, protection,
  lastUsedAt) with raw JSON one toggle away. Accept: catalog section.
  **Not built.** The op emits `kind` and `publicLine` and the CLI/JSON
  shape is done; only the typed tile widget is outstanding.

## 4. Boundary ops + approval — §26, §27, §31

- [x] ⚙ **Engine pause-for-approval** — a step must be able to suspend its
  cell pending a UI decision. Reuse the `waiting-peer` pause machinery
  (design v2 §21a) rather than a new async model; the gate function is
  injected by the shell like `setClipboardReadGate`. Accept: engine test
  drives approve/deny/cancel through a stubbed gate.
- [x] `agent.sign` / `agent.decrypt` registered per §26f.
  **Deviation:** `format=raw` is dropped from the enum — nothing in the
  toolkit consumes a bare signature, and emitting one invites pasting it
  somewhere load-bearing. **Deviation:** the "request N of M" counter is
  per run, not per cell; the engine does not thread a cell index into op
  bindings, and the run-level count is the number the loop defence needs.
  `recipeNeedsMainThread` covers them. Accept: verb smoke (with a stubbed
  gate + WebAuthn PRF stub, the `agent.save protection=passkey` precedent);
  sign output for a pgp key verifies via `gpg.verify`, for an ssh key via
  `ssh.verify`; `agent.decrypt` on an ssh key fails with the §31c
  wrong-kind message.
- [x] Approval banner widget — §27b contents (step text, key identity,
  payload digest + text preview, namespace/mode), §27c actions
  (Deny / Approve once / session checkbox), §27d loop counting + per-run
  batch. Accept: catalog states for single, loop (1-of-N with batch
  offer), passkey, decrypt, and denied; measured tones (`--warn` left
  border) and button weights.
- [x] RunBar `waiting-approval` state. Accept: catalog state; live run
  pauses at the cell and Cancel resolves as deny with the §31c cancel
  message.
- [x] Session grants — store keyed (key id × sign|decrypt), TTL tied to
  `VAULT_SESSION_TTL_MS`, revoked by Lock / Clear sensitive / idle scrub;
  Keyring row shows `approved · N uses · countdown`. Accept: unit tests
  for scope + expiry; counter increments on a granted run (live check).
- [x] §26c `data-key-exposed` chip treatment — underline on
  `agent.unlock` chips and every chip consuming the exposed slot
  (slot-graph walk). Accept: computed style shows the `--warn` underline
  on exactly the consumer set for a fixture recipe; no underline on
  boundary-op chips.
- [x] ⚙ `exposure: "exports-secret"` StepSpec field + ToolCard warn chip;
  `agent.unlock` doc steer updated. Accept: ToolCard catalog state; chip
  absent on `agent.sign`'s card.
- [x] Shelf split — `boundary` shelf (order 0) + `vault` (order 1), glyphs
  `agent-boundary` / `agent-sign` / `agent-decrypt`. Accept: drawer order
  measured; glyphs render.
- [x] Error strings per §31c, asserted verbatim in tests (the
  `share-check.js` wording-is-the-feature precedent).
- [ ] CLI `--approve <id>[:sign|decrypt]` / `--approve-all`.
  **Not shipped, and not merely deferred:** `basilisk run` refuses the
  whole `agent` toolbox at pre-flight (exit 4, verified) because Node has
  no IndexedDB vault, so a boundary op never reaches the gate and the flag
  could not take effect. It becomes buildable when the CLI key store
  lands. The gate's no-surface message says this host has nowhere to ask
  and points at the toolkit page, rather than naming an inert flag.

## 5. CLI socket agent — §30c, §27f

- [x] `basilisk agent --ssh` — draft-miller-ssh-agent over
  `$SSH_AUTH_SOCK` / `\\.\pipe\openssh-ssh-agent`; request-identities +
  sign-request backed by loaded keys. Accept: `SSH_AUTH_SOCK=… ssh-add -l`
  lists the keys; a real `ssh`/`git` signature made through it verifies.
  (Needs a developer-run integration check on both socket flavors — CI
  can cover the protocol layer with an in-process client.)
- [x] Per-key `confirm`: TTY prompt per signature; no TTY →
  `SSH_AGENT_FAILURE` + stderr line, agent stays up. Accept: protocol
  test with a stubbed TTY; failure response asserted.
- [x] `basilisk agent --status` per §30b format. Accept: output asserted;
  states "not running" cleanly when the socket is absent.
- [x] **Deliberately absent**: any browser surface claiming CLI agent
  state (§30b). Accept: nothing to build — guard is review, not code.

## 6. Verified how

Browser measurement covered what renders without hover: the SSH drawer
between age and Agent with a verified CAST light, the Agent drawer with
Boundary above Vault, glyph coverage with no `#` fallbacks, and a clean
console under the production CSP. The ToolCard exposure chip and the
approval banner are pinned at source level instead — this session's
browser pane reported a 0x0 viewport, so Radix hover tooltips and
screenshots could not be exercised. Re-check both live when a working
pane is available.

The ssh-agent was verified against a real Windows named pipe (identities
listed, ed25519 signature verified by an in-process client). `ssh-add -l`
and a `git` signature through the agent remain developer-run checks on
both socket flavours.

## 7. Deferred (tracked, unscheduled)

- `bcrypt_pbkdf` — encrypted openssh-key-v1 read/write, and with it
  passphrase-mode vault protection for ssh/raw kinds (§28b, §29f).
- Mesh forwarding of approvals (§30d) — roster `role: "agent"`, sealed
  sign-request envelopes, Connections-pinned approval banner. Blocked on
  the live two-browser mesh verification still open in HANDOFF.
- `allowed_signers` principal lookup; SSH certificates; md5 fingerprints
  (the last is omitted on purpose, not deferred).
