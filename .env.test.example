# Committed E2E / CI defaults (no secrets). Copy to .env.test locally or use scripts/prepare-ci-env.sh.
BASILISK_BASE_URL=http://localhost:8080
BASILISK_DEV_APPROVE=1
BASILISK_TOKEN_SECRET=ci-test-secret
