# Deprecated

The Vite app under `web/` (built to `web/dist/`) is the only supported portal.

This `web/static/` tree is kept temporarily for reference and will be removed.
Do not edit these files — changes will not be served.
